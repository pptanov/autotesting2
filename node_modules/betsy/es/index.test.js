import { EventEmitter } from './';
var emitter;
beforeEach(function () {
    emitter = new EventEmitter();
});
describe('Betsy', function () {
    test('should emit to no listeners', function () {
        expect.assertions(0);
        emitter.emit('test', { test: true });
    });
    test('should receive event from subscription', function () {
        expect.assertions(1);
        emitter.on('test', function (data) {
            expect(data).toEqual({
                test: true,
            });
        });
        emitter.emit('test', { test: true });
    });
    test('should receive events  from subscription', function () {
        expect.assertions(3);
        emitter.on('test', function (data) {
            expect(data).toEqual({
                test: true,
            });
        });
        emitter.emit('test', { test: true });
        emitter.emit('test', { test: true });
        emitter.emit('test', { test: true });
    });
    test('should receve only one event from once', function () {
        expect.assertions(4);
        emitter.once('test', function (data) {
            expect(data).toEqual({
                test: true,
            });
        });
        emitter.on('test', function (data) {
            expect(data).toEqual({
                test: true,
            });
        });
        emitter.emit('test', { test: true });
        emitter.emit('test', { test: true });
        emitter.emit('test', { test: true });
    });
    test('should receve only one event when not the first subscription', function () {
        expect.assertions(7);
        emitter.on('test', function (data) {
            expect(data).toEqual({
                test: true,
            });
        });
        emitter.once('test', function (data) {
            expect(data).toEqual({
                test: true,
            });
        });
        emitter.on('test', function (data) {
            expect(data).toEqual({
                test: true,
            });
        });
        emitter.emit('test', { test: true });
        emitter.emit('test', { test: true });
        emitter.emit('test', { test: true });
    });
});
//# sourceMappingURL=index.test.js.map